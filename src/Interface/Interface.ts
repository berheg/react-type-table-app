export interface dataType {
  id: string;
  name: string;
  gender: string;
  company: string;
  email: string;
  isActive: string;
  picture: string;

  }

  export interface columnType {
    Header: string,
    accessor: string,
    }
